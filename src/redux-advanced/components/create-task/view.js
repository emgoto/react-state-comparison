import React from 'react';
import CreateTaskView from '../../../common/components/create-task';

const CreateTask = ({ createTask }) => <CreateTaskView onCreate={createTask} />;

export default CreateTask;
